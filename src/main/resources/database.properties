# Database Configuration
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/hotel_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.username=root
db.password=root

# Backup Database Configuration (failover + резервная копия)
# Пример: PostgreSQL как резервная СУБД
db.backup.driver=org.postgresql.Driver
db.backup.url=jdbc:postgresql://localhost:5432/hotel_db_backup
db.backup.username=postgres
db.backup.password=postgres

