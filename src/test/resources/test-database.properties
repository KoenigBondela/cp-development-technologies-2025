# Test Database Configuration (H2 in-memory)
db.driver=org.h2.Driver
db.url=jdbc:h2:mem:primarydb;DB_CLOSE_DELAY=-1;MODE=MySQL
db.username=sa
db.password=

# Backup Database Configuration (H2 in-memory second DB)
db.backup.driver=org.h2.Driver
db.backup.url=jdbc:h2:mem:backupdb;DB_CLOSE_DELAY=-1;MODE=MySQL
db.backup.username=sa
db.backup.password=

